---
name: 手机上可以用了-但是怎么部署到vercel上呢
description: 手机上可以用了 但是怎么部署到vercel上呢 — verified by 3 checks
triggers: ['schema', 'prisma', '.env', '手机上可以用了']
---

# 手机上可以用了-但是怎么部署到vercel上呢

> 自动从会话 05a95fc4 蒸馏的草稿。审核后用 `/skill approve 手机上可以用了-但是怎么部署到vercel上呢` 入库，或 `/skill reject 手机上可以用了-但是怎么部署到vercel上呢` 丢弃。

## Steps
1. 阅读 / 搜索：npm install @prisma/adapter-libsql @libsql/client、E:\vibe-coding\Tianshu\src\lib\prisma.ts、E:\vibe-coding\Tianshu\prisma\schema.prisma
2. 修改：E:\vibe-coding\Tianshu\prisma\schema.prisma
3. 阅读 / 搜索：grep -q "DATABASE_URL" .env 2>/dev/null && echo "e
4. 修改：E:\vibe-coding\Tianshu\src\lib\prisma.ts、E:\vibe-coding\Tianshu\.env.example
5. 阅读 / 搜索：grep -q "dev.db" .gitignore 2>/dev/null && echo "a
6. 操作：job
7. 验证：npx prisma generate 2>&1 && npx tsc --noEmit 2>&1
8. 修改：E:\vibe-coding\Tianshu\src\lib\prisma.ts
9. 验证：npx tsc --noEmit 2>&1
10. 阅读 / 搜索：node -e "const m = require('@prisma/adapter-libsql
11. 修改：E:\vibe-coding\Tianshu\src\lib\prisma.ts
12. 验证：npx tsc --noEmit 2>&1 && npx vitest run 2>&1
13. 阅读 / 搜索：git add -A && git commit -m "feat: 支持Turso数据库 + Ve

## Verified by
- cd /e/vibe-coding/Tianshu && npx prisma generate 2>&1 && npx tsc --noEmit 2>&1 (passed 0)
- cd /e/vibe-coding/Tianshu && npx tsc --noEmit 2>&1 (passed 0)
- cd /e/vibe-coding/Tianshu && npx tsc --noEmit 2>&1 && npx vitest run 2>&1 (passed 0)

<!-- skill-draft-key: 70e808a30fb7 -->
<!-- source-session: 05a95fc4 -->
